# Foundry Day-8/9 Execution Prompt — Auth0: the badge reader at the counter

> **Generated**: 2026-09-16
> **Purpose**: Daily kickoff prompt for Days 8-9 (Sprint 2 opens). Say "Read `tech-foundry/docs/prompts/day-08-auth0-2026-09-16-2106.md`" to restore full context.
> **Master Rules**: `tech-foundry/docs/prompts/master.md` (all rules apply in full — esp. 16: ask before assuming, simple English + restaurant analogy, crisp explanations)
> **Roadmap Reference**: `tech-foundry/docs/roadmaps/v4/foundry-roadmap-v4-2026-08-11-1840.md`
> **ADR Reference**: `tech-foundry/docs/adrs/` — ADR-001 (dual-language), ADR-004 (Fastify/FastAPI), ADR-007 (error contract), ADR-010 (CI ships cellar-less)
> **Prior Day**: `tech-foundry/docs/notes/day-07-ci-2026-09-15-1151.md`

---

## Day Definition (Roadmap v4, Sprint 2 — real users can sign up, log in, and pay, Days 8-9)

> | 8-9 | Auth0 signup/login | Campaign API (Gatekeeper RBAC) | Auth0 hosted login, JWT in requests |

The restaurant has a **public street** (health), **counters** (data routes), a **receptionist** (error contract), and an **inspector** (CI). What it lacks: *only the right guests reach the counter*. This day installs a **badge reader** at the door. Auth0 is the badge *printer*; our apps never print — they only *read*.

## Pre-Read Checklist (do this before generating the breakdown)

1. Read all 16 rules in `docs/prompts/master.md`.
2. Read Roadmap v4 Sprint 2 table — Days 8-9 **and** 10-11. Ownership/RBAC (owner invites, member views) rides on this same token; the door installed today must leave room for *roles* tomorrow without re-hanging.
3. Read `docs/notes/day-01` through `day-07` in order. Day-07's preview is the contract for this day — and note ADR-010 twice: the inspector runs **cellar-less**, and the 500 family is welded to that fact. Today's change must keep both CI jobs green *and* let tests reach *past* the new door (see "The Door In Tests").
4. Skim ADR-004 (the framework pair) and ADR-007 (the 4-field envelope shape). 401 becomes a **new resident** of the contract — the same status as 409, which was "reserved, wired but unused"; 401 gets defined, wired, *and tested* (the contract grows a line, both kitchens, same script).
5. Inspect current state: routes in both tracks; **how config actually enters the runtime today** — the apps read `DATABASE_URL` from the *environment* (`py/src/db.py`, `ts/src/db.ts`), and *the only injector is the compose base's `environment:` block* (`docker-compose.yml`); **no `.env` files exist and neither app loads one** (no `dotenv`/`python-dotenv`), and **neither Dockerfile declares app-config env**. The deploy (ADR-008, day-6) ran `docker compose -f … -f docker-compose.prod.yml up` — the *cellar* is doorless (no 5432 host binding), but the *app* containers still get their env from that same `environment:` block, which the prod overlay **inherits untouched** (it `!reset`s `volumes`/`ports`/`command`, never `environment:`). So the single env lever is compose's `environment:` — and note the CI suite's existing tests (**which routes do the 404/422/500 tests hit?**) drive everything below.
6. Confirm the live context: the deployed 8000/8001 on your AWS box (`54.208.101.60`) are real and live; the Auth0 tenant is a **real free-tier account you create** — an action the breakdown must hand to the user with exact steps (a real account, zero cost, not something I do on their behalf).

## Today's Deliverables (the ask)

### A. Code-side changes (both tracks, in parallel)

1. **Verify, never mint.** The hosted tenant signs RS256 JWTs; the app verifies against its public JWKS set (`/…/jwks.json`). Pick the reader per track with a stated reason (the day-7 style: options presented, breakdown picks):
   - **TS**: `fastify-jwt` configured for JWKS/RS256 — or the Auth0 Node SDK/action. One or the other, with the *why* (parity bar below).
   - **PY**: a JWKS-verifying library (e.g. `python-jose` + a JWKS fetch-with-cache — or an Auth0-specific package). Same question, Python's door.
   - **Parity bar (ADR-001)**: the *same five* rejections produce the *same* 401 envelope on 8000 and 8001 — no token, malformed, expired, wrong audience, wrong issuer. The door speaks one language at both counters.
2. **401 into the ADR-007 contract, both tracks**: `401` → `{ statusCode: 401, error: "Unauthorized", message: <which check failed> }` in each track's global handler (the receptionist's script gains a line) — plus a 401 contract test in the CI suite (that's the payoff; section B).
3. **Public vs. protected**: the **street stays public by design** — health, and the OpenAPI/Swagger docs (*the menu — a guest may read the menu without a badge*; state this rationale, or argue for locked docs, one line each). All data routes sit behind the door. **Decide the mechanism**: a global hook/middleware with a small **public allowlist** (recommended — one choke point, same spirit as the ADR-007 single front desk) vs per-route registration (a door per room; the allowlist then lives anywhere the routes do). The ADR records whichever wins.
4. **Tenant environment, all the way to production** — through *the lever that exists* (item 5): add `AUTH0_DOMAIN` + `AUTH0_AUDIENCE` to compose's `environment:` so **both app tracks** get them (`db-push` needs only `DATABASE_URL` — leave it alone), and the prod overlay **inherits** them → zero Dockerfile changes, zero `.env` files, dev and prod stay one block apart. The open sub-decision is *dev-mock vs prod-real*: today they share **one** `environment:` value. Per rule 16 the breakdown presents how prod gets *real* tenant values without breaking that shape — a **prod-overlay** `environment:` (a *new* concept; the overlay has never carried env, but it is exactly ADR-009's "stronger than base" pattern) vs the user shell-exporting a different value for a direct local `uv run uvicorn` / `pnpm start`. ADR-008 constrains **only the cellar's port** — it does *not* forbid a prod-only env value, and it does *not* mean a `docker run` deploy (day-6 used `compose up`); do **not** let the doorless story rewrite the deploy. The deployed counters must receive the two values or they 401 *everyone* — a **user-side config action**.
5. **The one new module per track**: `py/src/auth.py` / `ts/src/auth.ts` — verify + the allowlist + the 401 throw. One file each is right at this thickness; a `middleware/` directory is a *make-it-right* move (say so, don't do it).

### B. Test-side (the inspector keeps zero cellars, ADR-010 is not negotiable — and it *grows*)

1. **The door is a testability gift — collect it.** Today, an unauthenticated `GET /workspaces/{fake-id}` walks into the cellar and, in cellar-less CI, 500s — that's the *untestable DB path* Day 7 named. With the door, the *same request dies at 401 before any cellar*. So the CI suite (still cellar-less, still green) gains a **401 contract test against a real data route** — precisely the shape it couldn't reach before. Both tracks, symmetrically.
2. **Sort the existing suite against the door, honestly**: the 404/422 tests aim *upstream* of the door (missing routes, malformed bodies — unregistered paths 404 at the router; validation fires before auth in the FastAPI-dependency pattern) → they **survive untouched**. The **500 family sits *downstream*** of the door → it must be *given a pass*. **The decision (see "Decide First")**:
   - (a) a test-mode flip (`AUTH0_DISABLED` / a test-secret mode in CI) — simple, but CI then tests the 500 through a door that's *off*; the welded test depends on the door's absence. Name it if picked.
   - (b) a **genuinely signed test token** (HS256 test secret, or RS256 vs a fixture JWKS) that the door *accepts* — the door is always on in CI; the 401 test covers the door layer, the 500 test covers the cellar layer, each layer tested in itself. The stronger choice; costs a fixture.
   - Pick one, state the ADR-010 consequence in one sentence, keep both the 401 *and* 500 tests in both tracks — the *only* permitted asymmetry is one that is *named*. The fixture that mints the valid token (secret, kid, aud, iat/exp — all of them) must be specified in full: every downstream test depends on it (rule 7, self-containment).
3. **The local bar keeps day-7's shape**: `uv run pytest -q` and `pnpm test` must still pass with **zero services** — the conftest `:9` trick unchanged; the door stands in front of the dead cellar, behind it is exactly as before.

### C. Documentation side (rule 12, day-6/7 structure, all of it)

1. **Notes file** `docs/notes/day-08-auth0-[yyyy-mm-dd-hhmm].md`: built / learned / broke-&-fixed / **Outcome** (not "Blocked" — day 7 taught the branch-state discipline: state what the branch *actually* is) / commands run by you vs. me, with outcomes / ADRs created / Preview of Next Step.
2. **ADR-011** (the day's decisions — pick the sharpest per rule 11; the ADR captures what *happened*, including asymmetries, not what was planned). Candidates: verify-not-mint on both tracks; 401 as the second ADR-007 line; public-allowlist vs per-route; the test-access choice (B2) with its ADR-010 consequence.
3. **Draft Day 10-11's prompt file** (`docs/prompts/day-10-…`) per master.md's day-close step 6 — workspace ownership + basic RBAC (Plane's workspace-members model: owner invites, member views; it reads `sub`/roles *claims from the token this day installs* — the door is the only reason days 10-11 can reason about *who* is at the counter). **Notes + ADR-011 + next prompt file = one closeout commit**; the user presses commit (rule 13).

## Decide First (shapes the whole day — settle before generating the breakdown)

1. **How tests get past the door in CI** — B2 (a) vs (b). The only decision of the day that can touch the ADR-010 zero-service guarantee; everything else is independent of it.
2. **The public allowlist's exact membership** — health + docs + (the root route? decide, one line per path in the ADR).
3. **Where prod-specific tenant values live** — the compose base `environment:` (one value feeding both worlds) vs a **prod-overlay** `environment:` (ADR-009's "stronger than base" pattern applied to env for the first time) vs the user shell-exporting for a direct local run. ADR-008 governs the *cellar's port binding only* — it neither forbids a prod-only env value nor implies a `docker run` deploy. This is a **live change on real infra** → present the real options, *you* pick (rule 16).
4. **The real tenant** — exactly what you create (an Application with an audience; the client ID is the audience for machine-to-machine), and the two values that land in the env: `AUTH0_DOMAIN` = your tenant's domain, `AUTH0_AUDIENCE` = the Application's audience identifier (`<domain>/…`). The breakdown states account creation as user-side steps; I never create accounts on their behalf.

## Format (rules, applied to this day)

- Complete pseudocode, no real code (rule 3) — **except** the two JWT-verify snippets and the compose base `environment:` line (+ prod-overlay `environment:` **only if** Decide-3 lands it there) are *infrastructure-as-code* the user will copy; flag those "this one is real, everything else is prose" (day-7 precedent).
- File/folder tree diff at repo root (rules 4-5): `+py/src/auth.py`, `+ts/src/auth.ts`, the compose base `environment:` additions (+ a prod-overlay `environment:` **only if** Decide-3 lands it there), `+py/tests/test_auth.py` / `+ts/tests/auth.test.ts` — one-line "why this location" each. **No new `.env` file belongs to this day** (none exists, none is loaded); if Decide-3 does yield a user-side file for direct local runs, name it and mark it **local-only, gitignored**.
- Scaffolding commands for both tracks (rule 6): the real package names (`uv add …` / `pnpm add …`); state that both land in the **committed lockfiles** — that's how `--frozen` stays honest in CI.
- Deprecation/upgrade notes (rule 8): flag where day 10-11 *roles* plug in (a claim read at the existing door — no new framework); flag "custom JWT signing" as the roadmap's *later* move (Auth0 → custom JWT is the "learn later" arc) — named, not done.
- End with the **Day 8-9 bar** (rule 9): user runs → tenant created → the compose `environment:` carries `AUTH0_DOMAIN` + `AUTH0_AUDIENCE` (dev and prod; + a gitignored local file **only if** Decide-3 lands one) → local `uv run uvicorn` / `pnpm start` → `curl 8000/workspaces` **without** token expect the 401 envelope; **with** token expect 200 on 8000 *and* 8001 → both suites pass → **push** → both GitHub jobs green (the inspector checks the badge *and* the welded 500 still reaches its dead cellar) → deployed cell updated → the **real** 8000/8001 behind the **real** door. That is the day: the street stays public, the counter goes private, the contract gained one line, the inspector kept its zero cellars.
- Preview Days 10-11 (Ownership + basic RBAC) per C.3; ADR-011 per C.2; notes per C.1.
- Never commit: stage, show the diff summary, propose the message, the user commits (rule 13). Suggested shape: one code commit per track **or** one combined "feat: Day-8/9 auth (both tracks)" — ask.
- Docs are the source of truth, nothing into memory (rule 14); `docs/scraps/` untouched (rule 15); ask before assuming, keep it crisp, and when a word is technical, reach for the restaurant (rule 16).

## Start With

> "Read `tech-foundry/docs/prompts/day-08-auth0-2026-09-16-2106.md` and `tech-foundry/docs/roadmaps/v4/foundry-roadmap-v4-2026-08-11-1840.md`, then give me what's next."

(The AI then runs the Pre-Read Checklist, **asks the four "Decide First" questions first** (rule 16), and emits the full Day 8-9 breakdown in the format above.)
